require 'spec_helper'

describe FancyCards do
  it 'has a version number' do
    expect(FancyCards::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
